PORT=8787
DATABASE_URL=postgresql://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres?sslmode=require

# Optional, only if you expose the simulator from a public tunnel.
PUBLIC_BASE_URL=http://localhost:8787
